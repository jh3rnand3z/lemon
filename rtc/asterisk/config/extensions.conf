[general]
static=yes
writeprotect=no
extenpatternmatchnew=no
clearglobalvars=no
autofallthrough=no



[transmision]

exten = _XXXX!,1,Wait(0.1)
  same = n,Dial(SIP/2501/${EXTEN},45)
  same = n,Hangup(

exten = 20XX,1,Wait(0.1)
  same = n,Dial(sip/${EXTEN})
  same = n,Hangup()


  exten = 2010,1,Answer()
  same = n,Wait(0.5)
  same = n,Dial(sip/2010,45)
  same = n,Hangup()

exten = 2020,1,Answer()
  same = n,Wait(0.5)
  same = n,Dial(sip/2020,45)
  same = n,Hangup()

exten = 2030,1,Answer()
  same = n,Wait(0.5)
  same = n,Dial(sip/2030,45)
  same = n,Hangup()

exten = 2040,1,Answer()
  same = n,Wait(0.5)
  same = n,Dial(sip/2040,45)
  same = n,Hangup()

exten = 2050,1,Answer()
  same = n,Wait(0.5)
  same = n,Dial(sip/2050,45)
  same = n,Hangup()

exten = 2060,1,Answer()
  same = n,Wait(0.5)
  same = n,Dial(sip/2050,45)
  same = n,Hangup()

exten = 2070,1,Answer()
  same = n,Wait(0.5)
  same = n,Dial(sip/2050,45)
  same = n,Hangup()
